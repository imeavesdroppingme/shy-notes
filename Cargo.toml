[workspace]
resolver = "2"
members = [
  "crates/interaction-core",
  "src-tauri",
]

[workspace.package]
edition = "2021"
license = "GPL-3.0-only"
version = "0.1.7"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
