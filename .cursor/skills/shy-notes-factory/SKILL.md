# shy-notes factory

Advance one phase at a time. English artifacts only. Local agents only.

## Paths

- Core: `crates/interaction-core`
- Native: `src-tauri`
- UI: `src`
- Contracts: `docs/contracts/interfaces.md`
- ADRs: `docs/ADR/`
- Local CI: `scripts/ci-local.sh`

## Commands

```bash
./scripts/ci-local.sh
pnpm tauri dev
pnpm tauri build
```

## Escalation

Stop and ask the user if Phase 2 global mouse / window move fails on an MVP OS (Electron vs Qt pivot), or before enabling paid signing.
